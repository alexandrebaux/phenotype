var valeurCourante = 156;
var valeurPafaite = 1990;

var monPremierPhenotype = phenotype(function(genome) {

    // Quand monPremierPhenotype.run() est executé

        // On change la valeur courante
        // genome() retourne un nombre à virgule entre 0 et 1
        valeurCourante = genome() * 100000; 
 
});

// Ici on répéte le code 10000 fois.
for (var i=0; i < 10000; i++) {

    // On affiche la valeur de la variable valeurCourante. 
    // La première fois (valeurCourante = 156)
    // par la suite la valeur vas ce rapprocher de la variable valeurPafaite.
    console.log(valeurCourante);

    // On execute la fonction présente dans le phenotype ci-dessus.
    // Ainsi la variable valeurCourante prend une certaine valeur.
    monPremierPhenotype.run()

    // On calcule la distance entre la bonne valeur est la valeur courante
    var errorValue = Math.abs(valeurCourante - valeurPafaite);
    monPremierPhenotype.error(errorValue);    

}

